# Aviator Analyzer API

This backend is prepared for an authorized live data provider.

It does NOT generate, fake, or predict game outcomes.

When an authorized provider/API is available, the backend can be extended to receive verified round data and pass it to the existing dashboard.
